# 每日工作報告

> 日期：2026/06/17

> 姓名：陳洺安

> 涵蓋專案：SmartCompeqQuery 、Vision AI

---

## 一、今日工作摘要

* 完成 PatchCore 精準度改善，包含 448px 解析度、L2 正規化、k=3 KNN、NG 優先閾值策略與操作點表格輸出。
* 完成 PatchCore 驗證集與測試集評估，並確認 AUROC 落差主因為 OK 訓練資料與 NG 測試資料來自不同攝影機系統。
* 持續進行監督式分類器重訓，已完成內部 split 訓練與最佳 epoch 選定，尚待測試集驗證。
* 修復 Vision AI 前端 YOLO 匯出 403 問題，改以 fetch 帶入 Authorization token 下載檔案。
* 修復無標註資料時前端顯示紅色錯誤的問題，改為黃色警告「未標註資料」。

---

## 二、今日完成事項

| 項目 | 工作內容 | 完成狀態        | 備註 |
| -- | ---- | ----------- | -- |
| 1  | PatchCore 精準度改善：解析度提升至 448px、加入 L2 正規化、KNN 改為 k=3、調整為 NG 優先閾值策略，並新增 mode_test 與操作點表格輸出 | 已完成 | 主要異動包含 `patchcore_connector.py`、`inspect_heatmap.py`、`CLAUDE.md` |
| 2  | 建立新版記憶體庫 `patchcore_v4_448.pkl`，共 166,521 patches，並加入 `normalize: True` 旗標 | 已完成 | 供 PatchCore 新版流程使用 |
| 3  | 完成 PatchCore 驗證集與測試集評估，驗證集 AUROC 0.8190、測試集 AUROC 0.7103 | 已完成 | 測試集 NG 漏判率 50%，OK 誤判率 27.5% |
| 4  | 完成 PatchCore 成效落差診斷，確認 OK 訓練集與 NG 測試集來自不同攝影機系統，造成特徵分佈無法對齊 | 已完成 | OK 舊系統 DS-CH250-CAM4；NG 新系統 CAM1S2POS0 |
| 5  | 監督式分類器重訓，改用 `graph\NG\Ng_validation_cropped` 15 張 NG 圖，並完成內部訓練/驗證切分與最佳 epoch 選定 | 進行中 | 尚待執行 `--mode test` 確認測試集表現 |
| 6  | 修復 Vision AI 匯出 YOLO 檔案時發生 403 的問題 | 已完成 | 改用 `fetch()` 帶 `Authorization: Bearer token` 後再觸發下載 |
| 7  | 修復無標註資料時顯示紅色 HTTP 404 錯誤的問題 | 已完成 | 偵測 404 後改顯示黃色警告「未標註資料」 |
| 8  | 完成今日 Git 推送流程 | 已完成 | 推送至 `5090_windows` 分支 |

---

## 三、進行中事項

| 項目 | 工作內容 | 目前進度 | 預計完成時間 | 需協助事項 |
| -- | ---- | ---- | ------ | ----- |
| 1  | 監督式分類器測試集驗證 | 已完成訓練與驗證切分，最佳 epoch 為 41，閾值 0.08；尚待執行 `--mode test` | （未定） | 需確認測試集實際表現 |
| 2  | 新系統資料補強 | 已確認目前 OK/NG 來源攝影機不同，導致分數分佈重疊 | （未定） | 需取得新系統 OK 圖；主管表示會取得更多 NG 標註圖 |

---

## 四、遇到的問題

| 問題描述 | 影響範圍 | 已採取行動 | 後續處理 |
| ---- | ---- | ----- | ---- |
| OK 訓練資料與 NG 測試資料來自不同攝影機系統，導致特徵分佈無法對齊，NG 與 OK 分數大量重疊 | PatchCore 測試集辨識效果，測試集 AUROC 降至 0.7103，NG 漏判率 50% | 完成驗證/測試評估與根本原因診斷 | 取得新系統 OK 圖，並視情況評估 ensemble 或 PatchCore NG 引導半監督改良 |
| YOLO 匯出使用 `<a>` tag 直接下載，無法帶登入 token，導致伺服器回 403 | Vision AI 標註匯出功能 | 改用 `fetch()` 帶 Authorization token 取得檔案後下載 | （無） |
| 圖片集無標註資料時後端回 404，前端顯示紅色錯誤訊息 | Vision AI 前端使用體驗 | 偵測 404 後改顯示黃色警告「未標註資料」 | （無） |

---

## 五、交接事項

* 今日程式變更主要涉及 `patchcore_connector.py`、`inspect_heatmap.py`、`CLAUDE.md` 與 `frontend/js/annotation-extensions.js`。
* 今日 Git 已推送至 `5090_windows` 分支。
* 權限等級整理：`view` 只能瀏覽、`user` 可新增修改、`admin` 可刪除、`is_admin` 為全域管理員且可管理帳號。

---

## 六、待辦/確認事項

* 執行監督式分類器 `--mode test`，確認測試集實際表現。
* 取得新系統 OK 圖，作為改善 PatchCore AUROC 的主要方向。
* 等待主管補充更多 NG 標註圖。
* 視測試結果決定是否採用 ensemble。
* 視情況評估 PatchCore NG 引導半監督改良。

---

## 七、其他補充

* PatchCore 目前主要瓶頸不是閾值設定，而是不同攝影機系統造成的資料分佈差異。