# 每日工作報告

> 日期：2026/06/24

> 姓名：陳洺安

> 涵蓋專案：SmartCompeqQuery、龍門缺版標註

---

## 一、今日工作摘要

* 修正訓練頁「測試模型」圖片 / 影片結果因授權 401 導致無法顯示或播放的問題。
* 新增推理檔案 Blob URL 取得流程，讓圖片與影片下載請求可帶入 Bearer token。
* 補上圖片測試與影片測試的處理中轉圈圈，改善使用者等待期間的回饋。
* 在匯入圖片、匯入資料集流程加入全域 Loading 遮罩、進度文字與逐張載入 log。
* 進行龍門缺版標註相關作業紀錄。

---

## 二、今日完成事項

| 項目 | 工作內容 | 完成狀態        | 備註 |
| -- | ---- | ----------- | -- |
| 1  | 修正「測試模型」圖片 / 影片推理結果無法顯示問題，新增 `getInferenceFileBlob()`，改以 `fetch` 帶授權標頭取得檔案後轉為 Blob URL | 已完成 | 前端修正，後端授權機制未改動 |
| 2  | 調整 `training.js` 圖片與影片推理結果顯示流程，包含 async/await、Blob URL 套用、關閉視窗時釋放 Object URL | 已完成 | 避免 `<img>` / `<video>` 無法帶 token 造成 401 |
| 3  | 測試模型流程新增處理中轉圈圈，圖片測試新增處理中彈窗，影片測試由靜態 emoji 改為一致的 spinner | 已完成 | 改善使用者等待體驗 |
| 4  | 匯入圖片 / 資料集流程導入全域 `Loading` 元件，包含資料集匯入、資料夾匯入與多檔圖片匯入進度顯示 | 已完成 | 含 ref-count 銜接、逐張進度文字與 console log |
| 5  | 龍門缺版標註作業 | 進行中 | 檔案僅記錄「標註龍門缺版」，未提供完成細節 |

---

## 三、進行中事項

| 項目 | 工作內容 | 目前進度 | 預計完成時間 | 需協助事項 |
| -- | ---- | ---- | ------ | ----- |
| 1  | 龍門缺版標註 | 已有當日作業紀錄 | （無） | （無） |
| 2  | 影片推理結果播放體驗後續評估 | 已知改用 Blob URL 後需整支影片下載完才能播放 | （無） | 若日後影片較大，需評估 token query string 與後端 Range 串流支援 |

---

## 四、遇到的問題

| 問題描述 | 影響範圍 | 已採取行動 | 後續處理 |
| ---- | ---- | ----- | ---- |
| 測試模型圖片 / 影片結果使用 `<img>` / `<video>` 直接載入，無法附帶 Authorization Bearer token，後端回傳 401 | 訓練頁「測試模型」圖片測試與影片測試結果視窗 | 改用 `fetch` 帶授權標頭取得檔案，轉成 Blob URL 後提供給圖片 / 影片元件 | 依驗證步驟確認 `/inference/{id}/source`、`/result` 回傳 200 |
| 匯入圖片或資料集時等待時間較長，使用者可能以為畫面卡住 | 標註工具的圖片匯入、資料夾匯入、資料集匯入流程 | 導入全域 `Loading` 遮罩、進度文字與逐張載入 log | 純前端改動，瀏覽器 Ctrl+F5 重整後驗證 |
| Blob URL 播放影片會失去原本後端 Range 串流優勢 | 影片推理結果播放 | 本次先以 Blob URL 解決授權與播放問題 | 若未來影片檔案較大，再評估後端支援 query token 與 Range 串流 |

---

## 五、交接事項

* 測試模型圖片 / 影片結果顯示修正後，需於正式環境登入後進入訓練頁驗證。
* 匯入資料集、匯入圖片資料夾、多檔圖片匯入皆需確認 Loading 遮罩是否正常顯示與關閉。
* 純前端異動，瀏覽器需 Ctrl+F5 重整後再測試。

---

## 六、待辦/確認事項

* 確認圖片測試結果視窗可正常顯示原始圖片與標註結果。
* 確認影片測試結果視窗可播放標註影片，且下載影片功能可用。
* 確認匯入資料集與匯入圖片流程的轉圈圈不會擋住合併 / 覆蓋對話框。
* 後續若影片檔案較大，評估是否改用支援 Range 串流的授權方案。

---

## 七、其他補充

* 今日另有「標註龍門缺版」紀錄，但檔案未提供更細項的標註數量或完成範圍。