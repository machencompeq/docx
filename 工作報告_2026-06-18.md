# 每日工作報告

> 日期：2026/06/18

> 姓名：陳洺安

> 涵蓋專案：SmartCompeqQuery、vision-ai

---

## 一、今日工作摘要

* 完成 vision-ai 標註匯出功能修正，改為前端本機打包，並補齊標準版 YOLO / YOLO-seg / COCO / VOC 匯出格式。
* 修復匯出 YOLO 時缺少下載進度回饋、無 `Content-Length` 時誤顯示 100%、未標註資料仍顯示進度提示等問題。
* 完成 SmartCompeqQuery 系統架構設計文件，定義 Report Contract、Metadata、Query Engine、權限、Log、API 
* 補充 SmartCompeqQuery 動態欄位來源需求，記錄查詢條件需支援 SQL 型態資料來源設定。

---

## 二、今日完成事項

| 項目 | 工作內容 | 完成狀態        | 備註 |
| -- | ---- | ----------- | -- |
| 1  | vision-ai 標註匯出功能修正：改為前端本機打包，只匯出已標註圖片；Pro 工具處理 YOLO / COCO，標準版補齊 YOLO / YOLO-seg / COCO / VOC 四種格式。另修復 YOLO 匯出進度顯示，改用 Streaming 讀取更新 toast，避免無進度回饋與 100% 誤顯示。 | 已完成 | commit：`11d147c`、`b036841` |
| 2  | SmartCompeqQuery 系統架構設計：完成平台定位、Report Contract、Input / Output Metadata、SQL Template、Query Engine、API、權限、Log、匯出、MVP 與開發階段規劃。 | 已完成 | 文件包含 FastAPI、MongoDB、Oracle DB、HTML/CSS/Vanilla JS 架構版本 |
| 3  | SmartCompeqQuery 動態查詢條件需求補充：記錄欄位來源需支援 SQL 表達方式，例如部門下拉選單由 SQL 查詢產生 `value` 與 `label`。 | 進行中 | 作為後續 Metadata / Input Source 設計需求 |

---

## 三、進行中事項

| 項目 | 工作內容 | 目前進度 | 預計完成時間 | 需協助事項 |
| -- | ---- | ---- | ------ | ----- |
| 1  | SmartCompeqQuery / Smart eVision 動態 SQL 選項來源設計 | 已記錄 SQL source 結構範例，待後續納入 Metadata 與後端 options API 實作 | （未定） | （無） |
| 2  | SmartCompeqQuery 落地 | 已完成架構設計與第一版範圍規劃，尚未進入實作 | （未定） | （無） |

---

## 四、遇到的問題

| 問題描述 | 影響範圍 | 已採取行動 | 後續處理 |
| ---- | ---- | ----- | ---- |
| YOLO 匯出時使用 `response.blob()`，需等待整個檔案下載完成才有後續動作，導致匯出期間沒有進度回饋。 | vision-ai 標註頁匯出體驗 | 改用 `response.body.getReader()` 串流讀取，逐 chunk 更新 toast 文字。 | （無） |
| 後端未回傳 `Content-Length` 時，進度計算以 `1/1` 顯示，造成一開始就顯示 100%。 | vision-ai 匯出進度顯示 | 僅在有 `Content-Length` 時顯示百分比，否則只顯示已接收 MB。 | （無） |
| 無標註資料時仍短暫出現進度提示，體驗混亂。 | vision-ai 匯出流程 | 延後建立進度 toast，確認回應為 200 後才顯示；404 直接顯示「未標註資料」。 | （無） |

---

## 五、交接事項

* vision-ai 今日記錄顯示進行中事項為無。
* SmartCompeqQuery 已整理完整架構文件，可作為後續 MVP 實作依據。
* SmartCompeqQuery 後續需確認 SQL 型態選項來源如何與 Metadata、Options API、權限過濾整合。

---

## 六、待辦/確認事項

* 確認 Smart eVision 第一版是否以 PO 交貨逾期報表作為 MVP 起始報表。
* 確認動態 SQL 選項來源的安全規則，例如允許查詢範圍、bind parameter、權限過濾方式。
* 後續實作 Smart eVision MVP API：`GET /api/reports`、`GET /api/reports/{reportCode}/metadata`、`GET /api/reports/{reportCode}/inputs/{inputName}/options`、`POST /api/reports/{reportCode}/query`。

---

## 七、其他補充

* vision-ai 今日 commit 記錄：15:35 `11d147c`、16:30 `b036841`。